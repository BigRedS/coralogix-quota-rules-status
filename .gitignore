# Secrets
.env

# Compiled binaries
/quota-rules-status
/webui-bin

# Lambda build artifacts
/bootstrap
/function.zip

# OpenTofu / Terraform state and vars (may hold secrets)
deploy/tf/.terraform/
deploy/tf/.terraform.lock.hcl
deploy/tf/*.tfstate
deploy/tf/*.tfstate.*
*.tfvars
